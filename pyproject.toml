[project]
name = "protonwg"
version = "1.0.0"
description = "WireGuard TUI manager for Arch Linux (Proton VPN)"
requires-python = ">=3.9"
dependencies = [
    "textual>=0.47",
]

[project.scripts]
protonwg = "protonwg:main"

[tool.setuptools.packages.find]
where = ["."]
